# ld65: UnoDOS/IIGS boot stage -> a flat 512-byte block-0 image at $0800.
MEMORY {
    BLK0: start=$0800, size=$0200, type=ro, file=%O, fill=yes, fillval=$00;
}
SEGMENTS {
    CODE: load=BLK0, type=ro, start=$0800;
}
